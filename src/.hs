-- a) taken from skript
-- f x = m
-- h = h


data Fun = Vegeta | Goku
h = h
w = Vegeta

-- b)
f _ Vegeta = DragonBall
-- call this way: f h w

-- c)
g Vegeta _ = DragonBall
-- call this way: g w h

-- d)
j a Vegeta b = DragonBall
-- call this way: j h w h

-- e)
-- call this way: h